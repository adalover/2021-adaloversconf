import styled from '@emotion/styled';

const Separator = styled.div`
  height: 24px;
  width: 1px;
  background: var(--tertiary-color);
  margin: 0 var(--space-4x);
`;

export const Pipe = () => <Separator />;
